var github = "https://github.com/Ressobe";
var yt = "https://www.youtube.com/channel/UCcWXlahMIEBY9WR11VVELuQ";

var left_side = "<span class=left_side>"
var right_side = "<span class=right_side>"
var close_span = "</span>"

const help= [
	  left_side + 'clear' + close_span	+ right_side + 'clear terminal screen' + close_span,
	  left_side + 'projects' + close_span + right_side	+ 'display my projects' + close_span,
	  left_side + 'socials' + close_span + right_side + 'links to my socials' + close_span,
	  left_side + 'neofetch' + close_span + right_side + 'display info about my hardware' + close_span,
	  left_side + 'cmatrix' + close_span + right_side +	'matrix effect' + close_span,
	];

const socials = [
	  left_side + 'github' + close_span + right_side + github + close_span, 
	  left_side + 'youtube' + close_span + right_side + yt + close_span,
];

const banner = [
	  '.########..########..######...######...#######..########..########',
	  '.##.....##.##.......##....##.##....##.##.....##.##.....##.##......',
	  '.##.....##.##.......##.......##.......##.....##.##.....##.##......',
	  '.########..######....######...######..##.....##.########..######..',
	  '.##...##...##.............##.......##.##.....##.##.....##.##......',
	  '.##....##..##.......##....##.##....##.##.....##.##.....##.##......',
	  '.##.....##.########..######...######...#######..########..########',
]

const hardwareSoftwareInfo = [
	left_side + 'OS:' + close_span + right_side + 'Arch Linux' + close_span,
	left_side + 'Shell:' + close_span + right_side	+ 'Fish' + close_span ,
	left_side + 'Window Manager:' + close_span + right_side	+ 'Dwm' + close_span,
	left_side + 'Terminal:' + close_span + right_side + 'Alacritty' + close_span ,
	left_side + 'Terminal Font:' + close_span + right_side + 'Hack Nerd Font' + close_span,
];

const projects = [];

const whois = [];